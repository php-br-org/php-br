{% extends "::layout.html.twig" %}

{% block content %}
    <div class="row">
        <div class="large-12 columns">
            <h3>Artigos</h3> 
            <p>
                <a href="{{ path('novo_artigo') }}">Criar Artigo</a>
            </p>
            {% if artigos|length %}
                <table width="100%%">
                    <thead>
                        <tr>
                            <th>Titulo</th>
                            <th>Publicado por</th>
                            <th>Data</th>
                        </tr>
                    </thead>
                    <tbody>
                    {% for artigo in artigos %}
                        <tr>
                            <td>
                                <a href="/artigos/ver/{{ artigo.id }}">
                                    {{ artigo.titulo }}
                                </a>
                            </td>
                            <td>
                                {{ artigo.user.name }}
                            </td>
                            <td>
                                {{ artigo.dataPublicado|date('d/m/Y') }}
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            {% endif %}

        </div>
    </div>
{% endblock %}
