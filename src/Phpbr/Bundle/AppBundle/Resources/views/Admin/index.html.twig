{% extends "::layout_admin.html.twig" %}

{% block content %}
    <div class="row">
        <div class="large-12 columns">
            <ul>
                <li>
                    <a href="{{ url('admin_artigos') }}">{{ 'phpbr.header.articles'|trans({}, 'article') }}</a>
                </li>
                <li>
                    <a href="{{ url('admin_forum_categorias') }}">{{ 'phpbr.forum.forum_categories'|trans({}, 'forum') }}</a>
                </li>
            </ul>
        </div>
    </div>
{% endblock %}
