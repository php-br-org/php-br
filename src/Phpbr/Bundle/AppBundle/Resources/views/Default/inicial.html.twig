{% extends "::layout.html.twig" %}

{% block content %}
    <div class="row">
        <div class="medium-4 large-4 columns">
            {% for artigo in artigos|slice(0, 3) %}
                <h4>
                    <a href="{{ path('ler_artigo', { slug: artigo.slug }) }}">
                        {{ artigo.titulo|e }}
                    </a>
                </h4>
                <p class="text-justify">
                <img src="{{ artigo.user.email|email2gravatar('40') }}" alt="artigo.user.name|raw" class="th img-gravatar-inicial" />"
                {{ artigo.resumo|e }}
                </p>
            {% endfor %}
        </div>

        <div class="medium-4 large-4 columns">
            {% for artigo in artigos|slice(3, 3) %}
                <h4>
                    <a href="{{ path('ler_artigo', { slug: artigo.slug }) }}">
                        {{ artigo.titulo|e }}
                    </a>
                </h4>
                <p class="text-justify">
                <img src="{{ artigo.user.email|email2gravatar('40') }}" alt="artigo.user.name|raw" class="th img-gravatar-inicial" />"
                {{ artigo.resumo|e }}
                </p>

            {% endfor %}
        </div>

        <div class="medium-4 large-4 columns text-center">
            {% for artigo in artigos|slice(6, 3) %}
                <h4>
                    <a href="{{ path('ler_artigo', { slug: artigo.slug }) }}">
                        {{ artigo.titulo|e }}
                    </a>
                </h4>
                <p class="text-justify">
                <img src="{{ artigo.user.email|email2gravatar('40') }}" alt="artigo.user.name|raw" class="th img-gravatar-inicial" />"
                {{ artigo.resumo|e }}
                </p>

            {% endfor %}
        </div>
    </div>

    <div class="row">
        <div class="large-12 columns">
                {% block fos_user_content %}{% endblock %}
        </div>
    </div>

    <div class="row">
        <div class="large-12 columns">
            <hr />
            <h3>Notícias do mundo PHP</h3>
                <div class="medium-8 large-8 columns">
                    <h4></h4>
                </div>
                <div class="medium-3 large-3 columns">
                    <h4>Vagas de Emprego</h4>
                    <p>
                        - vaga 1 bla balblaba
                    </p>
                    <p>
                        - vaga 2 php blablalbalba
                    </p>
                    <hr />
                    <h4>Eventos PHP</h4>
                    <p>
                    dasdasda <br>
                    dasdasdas 
                    dasdas <br>
                    dasdasdasdasdas
                    </p>
                </div>
        </div>
    </div>
{% endblock %}
